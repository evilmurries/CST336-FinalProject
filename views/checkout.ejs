<% include partials/header.ejs %>

 <form method="get" action="/clearSession">
        <input type="submit" class="button" value="Cancel">
      </form>

 <% if(typeof emptyCart != "undefined" && emptyCart) { %>
              <p>
              Your Cart is Empty. Please Hit Cancel
              </p>
            <% } %>


 <% if(typeof emptyCart != "undefined" && !emptyCart) { %>

   <form method="get" action="/checkout">
        <input type="submit" class="button" value="Checkout">
      </form>

<div id="checkoutPets">
<table>
  <tr>
    <th>Pet Name</th>
    <th>Price</th>
    <th>description</th>
    <th>image</th>
  </tr>
<% data.forEach(function(row) { %>
      <tr>
      <th><%= row['pet_name'] %></th>
      <th><%= row['animal_type'] %></th>
      <th><%= row['adoption_fee'] %></th>
      <td><%= row['description'] %></td> 
        <td><img id='image' src='<%=row['image']%>' width='200' height='200'/></td>
      </tr>
<% }) %>
  </div>
 <p>
   
</p>

            <% } %>


<% include partials/footer.ejs %>